# Generated plist (rendered from template by install.sh, not tracked)
*.plist

# Logs
*.log
*.log.bak

# rclone config (contains OAuth tokens — never commit this)
rclone.conf

# macOS
.DS_Store
.Spotlight-V100
.Trashes

# Local test harness (scratch restic repos + fixtures, never committed)
.test-harness/

# Editor
.vscode/
.idea/
*.swp
*.swo
.superpowers/

# Claude Code — local & runtime files (shared settings.json / agents / commands stay tracked)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/*.lock

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
